# تاریخچه تغییرات

همه تغییرات مهم این قالب در این فایل ثبت می‌شود. قالب نسخه‌بندی معنایی (Semantic Versioning) را رعایت می‌کند.

## [1.0.1] — 1405/07/01 · 2026-09-23

رفع اشکال و بهبود تجربه کاربری بر پایه بازبینی کامل رابط. شماره‌گذاری مستقل از
نسخه قالب است؛ فایل‌های `dist/` و بسته تحویل با همین تغییرات بازسازی شده‌اند.

### رفع اشکال — تغییر زبان

- **صفحه‌های احراز هویت (۱۱ صفحه) و صفحه پیش‌نمایش**: کلید تغییر زبان فقط یک دکمه
  بی‌اثر بود و هیچ فهرستی برای انتخاب زبان نداشت. اکنون همان سوییچر فهرست‌دار هدر
  روی همه صفحه‌ها استفاده می‌شود و با یک کلیک، متن/جهت/عنوان صفحه عوض می‌شود.
- **بایند تکراری رویداد**: با هر تغییر زبان، شنونده کلیک سوییچر دوباره بسته می‌شد و
  یک کلیک چند بار `setLanguage()` را اجرا می‌کرد. بایندها محافظت‌شده‌اند.
- **علامت زبان فعال**: تیک کنار زبان جاری پس از هر تغییر جابه‌جا می‌شود.

### رفع اشکال — صفحه محصول و پیش‌نمایش

- **`preview.html`**: گالری دموها، چیدمان‌ها و پالت‌ها خالی می‌ماند. تابع `paint()`
  آرگومان مقصد را نادیده می‌گرفت و همه‌چیز را در میزبان عمومی صفحه می‌ریخت؛
  `paint(target, markup)` حالا هر دو شکل فراخوانی را پشتیبانی می‌کند (همین اشکال
  چیپ‌های فیلتر صفحه نتایج جستجو را هم از کار انداخته بود).
- **ساختار سند**: پارشال `preview.html` یک سند کامل بود و صفحه خروجی دو `<body>`
  و دو بلوک شخصی‌ساز داشت. پارشال به قطعه بدنه تبدیل شد، پوسته سند ویژگی‌های کامل
  (`data-theme`, `data-layout`, `data-section`) را می‌گذارد و `readBody()` هر پارشال
  دارای سند کامل را با هشدار باز می‌کند.
- **`index.html`**: بازنویسی کامل — همه بخش‌ها داخل `.landing-container`، منوی
  موبایل واقعی (دکمه همبرگری + `aria-expanded` + بستن با Escape)، شمارنده‌ها از
  سرویس، بخش‌های جدید چیدمان/کیت رابط/اعتماد و پانویس چهارستونی با کلیدهای تم،
  جهت و زبان.

### افزوده شد — احراز هویت

- بازطراحی کامل صفحه‌های ورود/ثبت‌نام/بازیابی/قفل با فیلدهای آیکون‌دار، نمایش
  رمز، سنجه قدرت رمز، جعبه حساب آزمایشی با پرکردن خودکار، دکمه‌های اجتماعی و
  یادداشت امنیتی؛ هر صفحه چیدمان اختصاصی خود را دارد.
- سرویس `authService` با قرارداد REST (login/register/forgot/reset/verify/logout/sessions)
  به‌عنوان نقطه اتصال بک‌اند.

### رفع اشکال — جدول‌ها، نمودارها، صفحات

- `.datatable__pager` و `.datatable__state` مارک‌آپ داشتند اما هیچ قاعده CSS نداشتند؛
  ردیف صفحه‌بندی بی‌استایل و پوشش حالت بارگذاری/خالی/خطا بدون جایگاه بود. هر دو
  استایل گرفتند و شماره‌های صفحه (۱، ۲، ۳) دکمه‌ای و خوانا شدند.
- سرستون‌های مرتب‌سازی نشانگر ندارند: آیکون دوحالته، `aria-sort` و رنگ ستون فعال
  اضافه شد؛ سلول‌ها `data-label` می‌گیرند تا در موبایل کارت شوند.
- نمودارهای داخل تب/کارت بسته با عرض صفر رسم می‌شدند؛ `ResizeObserver` نمودار را
  به‌محض گرفتن فضا دوباره رسم می‌کند.
- صفحات سیستمی (۴۰۳/۴۰۴/۵۰۰/تعمیر/به‌زودی/آفلاین)، FAQ، صفحات حقوقی و تغییرات
  محتوای واقعی گرفتند؛ صفحه‌های تنظیمات با معرفی بخش، فهرست مقادیر جاری و
  ذخیره‌سازی واقعی کامل شدند.
- صفحه‌های جزئیات بدون `?id=` رکورد نمونه را نشان می‌دهند (با نشان «رکورد نمونه»)
  و با شناسه نامعتبر پیام خطای روشن دارند.

### افزوده شد — کنترل کیفیت

- `npm run qa:toggles`: برای هر صفحه یک فرایند جدا اجرا می‌کند، واقعاً روی کلیدهای
  زبان و تم کلیک می‌کند و نتیجه را می‌خواند (`qa:all` شامل آن است).
- `tools/qa-classes.mjs`، `tools/qa-i18n.mjs` و حالت `--json` در `tools/smoke.mjs`.
- اجرای تازه: ساخت پاک، ۴۵٬۷۸۶ لینک بدون شکست، ۲۰۷/۲۰۷ صفحه با داده
  (۱۶۵ KPI، ۱۸۰ نمودار، ۱۰۲۹ ردیف)، ۲۰۷/۲۰۷ صفحه با کلید زبان و تم سالم،
  ترجمه‌ها بدون کلید گم‌شده.

## [1.0.0] — 1405/06/31 · 2026-09-22

اولین انتشار عمومی. این نسخه یک قالب کامل و آماده تحویل است.

### افزوده شد — هسته و سیستم طراحی

- توکن‌های CSS برای رنگ (رنگ اصلی + hover/active، ثانویه، موفق، هشدار، خطر، اطلاع، پس‌زمینه، سطوح، مرزها، چهار سطح متن)، تایپوگرافی، فاصله ۱ تا ۱۲، شعاع XS تا XL و سایه XS تا LG
- سه حالت رنگ (روشن، تاریک، سیستم) و شش پالت رنگی آماده: indigo، blue، emerald، violet، orange، rose
- شش چیدمان: سایدبار پیش‌فرض، مینی، جمع‌شده، افقی، دو ستونی و باکس‌دار
- پشتیبانی هم‌سطح RTL و LTR با ویژگی‌های منطقی CSS و تعویض پویا در زمان اجرا
- حالت‌های تراکم (راحت/فشرده)، اندازه فونت (S/M/L)، سبک سایدبار (ثابت/شناور/ریل) و انتخاب تقویم (شمسی/میلادی)
- فونت Vazirmatn با امکان تعویض؛ فونت در خروجی به `assets/fonts/` منتقل می‌شود
- ذخیره همه تنظیمات ظاهری در `localStorage` با پیشوند `nova:` و اعمال پیش از رندر (بدون پرش تم)

### افزوده شد — صفحات

- **۲۰۶ صفحه مستقل HTML**: ۱۰ داشبورد، ۷۸ صفحه اپلیکیشن، ۴۷ جدول فهرست، ۲۱ صفحه UI Kit، ۲۳ مستند، ۱۶ صفحه سیستمی، ۱۱ صفحه احراز هویت و صفحه محصول
- **۱۰ داشبورد تخصصی** با KPI، نمودار، جدول و جریان فعالیت اختصاصی: آنالیتیکس، فروشگاهی، CRM، SaaS، مالی، پروژه‌ها، منابع انسانی، پشتیبانی، هوش مصنوعی و لجستیک
- **کارگاه هوش مصنوعی** با ۱۲ فضا: چت، نویسنده، خلاصه‌ساز، بازتولید محتوا، کتابخانه پرامپت، استودیو تصویر، زمان‌بند، مصرف، مدل‌ها، کلیدهای API، تاریخچه و داشبورد AI
- **ماژول فروشگاه**: محصولات (فهرست، شبکه، جزئیات، ساخت)، دسته، برند، برچسب، انبار، دیدگاه‌ها، کوپن، سفارش‌ها با همه وضعیت‌ها و جزئیات سفارش
- **ماژول CRM**: مخاطبین، شرکت‌ها، سرنخ‌ها، معاملات، قیف فروش با کشیدن‌و‌رها کردن، فعالیت‌ها، تماس‌ها، جلسات، کمپین‌ها و پیش‌بینی فروش
- **پروژه و وظیفه**: فهرست، جزئیات، خط زمان، اعضا، فایل‌ها، کانبان با درگ‌ودراپ و وضعیت‌های backlog/todo/in-progress/review/done
- **تقویم** با چهار نما (ماه، هفته، روز، فهرست)، ساخت/ویرایش/حذف و جابه‌جایی رویداد، دسته‌بندی رنگی و پشتیبانی شمسی و میلادی
- **ایمیل**: صندوق، ارسالی، پیش‌نویس، ستاره‌دار، هرزنامه، زباله‌دان، جزئیات نامه و نامه جدید
- **گفتگو**، **اعلان‌ها**، **مدیریت فایل**، **کتابخانه رسانه**، **تیکت پشتیبانی**، **پایگاه دانش**
- **CMS**: صفحات، نوشته‌ها، دسته‌ها، برچسب‌ها، رسانه، دیدگاه‌ها و صفحه‌ساز (Page Builder)
- **مالی**: نمای کلی، تراکنش‌ها، حساب‌ها، هزینه‌ها، درآمد، پرداخت‌ها، فاکتورها، جزئیات و ساخت فاکتور، اشتراک‌ها و تسویه‌ها
- **گزارش‌ها**: فروش، درآمد، مشتریان، محصولات، مالی، عملکرد، ترافیک و پشتیبانی با خروجی CSV/اکسل/چاپ
- **تنظیمات** با ۱۲ بخش و **پروفایل** با ۱۰ بخش
- **صفحات عمومی**: قیمت‌گذاری، پرسش‌های متداول، تماس، قوانین، حریم خصوصی، مرکز راهنما، وضعیت سرویس، تغییرات، جستجوی سراسری و صفحه پیش‌نمایش زنده محصول
- **صفحات خطا**: ۴۰۳، ۴۰۴، ۵۰۰، تعمیرات، حالت آفلاین، به‌زودی، صفحه خالی و خطای ورود

### افزوده شد — کامپوننت‌ها و رفتارها

- کتابخانه ۳۹ کامپوننتی با توکن‌های طراحی، سازگار با هر شش چیدمان و هر دو جهت
- جدول داده کامل: جستجو، فیلتر چند‌فیلدی، مرتب‌سازی، صفحه‌بندی، نمایش/پنهان ستون، انتخاب سطری، حذف گروهی، خروجی CSV/اکسل/چاپ و حالت‌های بارگذاری/خالی/خطا/آماده
- فرم‌ها با هفت حالت (پیش‌فرض، فوکوس، پرشده، غیرفعال، فقط‌خواندنی، خطا، موفق، بارگذاری) و اعتبارسنجی فارسی شامل ایمیل، موبایل، کد ملی، کد پستی، شبا، گذرواژه و تطابق تکرار
- نمودارها روی ApexCharts با پوسته واحد، رنگ‌های توکن‌محور، پشتیبانی RTL و بازسازی خودکار هنگام تغییر تم
- کانبان با کشیدن‌و‌رها کردن و ذخیره ترتیب، تقویم، چت، پیام‌رسان با چهار موقعیت پیام، گفتگوهای تأیید، تایم‌لاین فعالیت و اسکلتون بارگذاری
- جستجوی سراسری و پالت فرمان (Ctrl + K) با جستجوی فارسی بدون حساسیت به ارقام
- میان‌برهای صفحه‌کلید و پنجره راهنما، ویرایشگر ابزارک داشبورد با ذخیره چیدمان

### افزوده شد — محتوا و بومی‌سازی

- سه زبان کامل فارسی، انگلیسی و عربی با تغییر زبان در زمان اجرا و بدون رفرش
- ارقام فارسی/عربی/لاتین، قالب‌بندی ارز (IRR, USD, EUR, AED) و اعداد فشرده
- تقویم شمسی بر پایه `jalaali-js` با تعطیلات رسمی و امکان سوئیچ به میلادی
- ۲۳ موضوع مستندات (مارک‌داون + صفحه HTML) و راهنمای نصب گام‌به‌گام
- دارایی‌های برداری اختصاصی: ۴ لوگو، ۲۴ آواتار، ۲۴ تصویر محصول و ۱۲ لوگوی برند

### افزوده شد — ابزارها و کیفیت

- زنجیره ساخت: تولید مستندات → تولید ۲۰۶ صفحه → تولید دارایی‌ها → نقشه سایت → باندل Vite → مرتب‌سازی خروجی
- `npm run qa:links`: بررسی ۴۵٬۰۰۰+ لینک و دارایی، پوشش کلاس‌های CSS در HTML و تمپلیت‌های JS، دسترس‌پذیری، شناسه‌های تکراری، SEO
- `npm run qa:smoke`: اجرای واقعی ۱۵۰ سناریوی صفحه در DOM شبیه‌سازی‌شده بدون نیاز به مرورگر
- `npm run package`: ساخت بسته تحویل با ساختار استاندارد (`html/`, `source/`, `documentation/`, `assets/`, `screenshots/`, `changelog/`)
- تولید خودکار `sitemap.xml` و `robots.txt`

### نکات فنی

- بدون jQuery، بدون `var` و بدون متغیر جهانی خارج از `window.NOVA`
- همه لینک‌های داخلی نسبی و سازگار با انتشار در زیرپوشه
- کد تقسیم‌شده (`manualChunks`) برای جدا کردن باندل نمودارها و گفتگوها از هسته
- حجم خروجی CSS حدود ۴۹۰ کیلوبایت (۶۳ کیلوبایت gzip) برای همه صفحات
