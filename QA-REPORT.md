# گزارش کنترل کیفیت نهایی — NOVAADMIN v1.0.0

این گزارش نتیجه اجرای واقعی زنجیره ساخت و کنترل کیفیت روی نسخه ۱٫۰٫۰ است.
دستور بازتولید: `npm run qa:all`

```bash
npm install            ✔ نصب وابستگی‌ها بدون هشدار مسدودکننده
npm run build          ✔ ۲۰۶ صفحه تولید و ۲۰۷ سند در dist مرتب شد
npm run qa:links       ✔ همه بررسی‌ها پاس شد
npm run qa:smoke       ✔ ۲۰۷/۲۰۷ صفحه با داده
npm run qa:toggles     ✔ ۲۰۷/۲۰۷ صفحه: تغییر زبان و تم کار می‌کند
npm run qa:responsive  ✔ بدون مانع ساختاری
npm run package        ✔ بسته تحویل ساخته شد
```

## ۱) ساخت و خروجی

| مورد | نتیجه |
| --- | --- |
| صفحه‌های تولیدشده | **۲۰۶** (۷ بخش، ۲۳ موضوع مستندات) |
| سند در `dist/` | **۲۰۷** (شامل `index.html` صفحه محصول) |
| بایت خروجی | ۲۴٬۹۹۱ کیلوبایت |
| دارایی‌ها | ۷۰ فایل + ۱ CSS + ۱۱ چانک JS |
| CSS | ۱ فایل ۵۶۳ کیلوبایتی (gzip ≈ ۷۲ کیلوبایت) |
| JS | ۱۱ چانک (`modules`, `apps`, `ai`, `content`, `vendor-*`) |
| بسته ZIP | ۶۷۰ فایل، ۳٫۹ مگابایت (فشرده) |

## ۲) بررسی لینک، دارایی و ساختار (`qa:links`)

| بررسی | نتیجه |
| --- | --- |
| لینک و دارایی درون صفحه‌ای | **۴۵٬۷۹۰** بررسی‌شده، **۰** شکسته |
| توکن‌های قالبی باقی‌مانده (`{{…}}`) | ۰ |
| کلاس‌های استفاده‌شده | ۴۵۵ کلاس، **۰** بدون استایل (از ۷٬۶۵۴ قاعده CSS) |
| آیکون‌ها | ۲٬۰۷۸ گلیف موجود، همه نام‌های ارجاع‌شده معتبر |
| شناسه‌های تکراری | ۰ |
| `<main>` و `<h1>` در همه صفحه‌ها | ✔ |
| تصویر بدون `alt` | ۰ |
| jQuery / `data-bs-*` / `var` | ۰ |
| نشانگرهای `console.log` / `debugger` | ۰ |
| فونت و تصویر مفقود در CSS | ۰ |
| `sitemap.xml` | ۱۷۹ URL + `robots.txt` |

## ۳) اجرای واقعی کنترل‌کننده‌ها (`qa:smoke`)

**۲۰۷ صفحه از ۲۰۷ صفحه** بدون خطا در DOM شبیه‌سازی‌شده اجرا شد — شامل صفحه محصول
`index.html`، ۱۰ داشبورد، ماژول‌ها، کارگاه هوش مصنوعی، جدول‌های داده، تقویم،
کانبان، چت، فاکتور، ۱۱ حالت احراز هویت، ۲۳ موضوع مستندات و صفحات سیستمی.

```text
pages booted   207/207
data rendered  165 KPI cards · 180 charts · 1029 table rows
thin pages     0
console errors 0
```

هر صفحه بر اساس «آنچه باید نشان دهد» داوری می‌شود (صفحه داده، صفحه نمونه، فرم،
متن، پیام وضعیت)، نه فقط با شمارش بایت.

## ۴) بازرسی کلیدهای سراسری (`qa:toggles`)

این بازرسی برای هر صفحه یک فرایند جدا اجرا می‌کند، سپس **واقعاً روی کلیدها کلیک
می‌کند** و نتیجه را می‌خواند: `lang`/`dir` سند باید عوض شود و متن گره‌های
`data-i18n` باید تغییر کند.

| بررسی | نتیجه |
| --- | --- |
| کنترل زبان در نوار بالای صفحه | **۲۰۷/۲۰۷** |
| ترجمه واقعی متن‌ها پس از کلیک | **۲۰۷/۲۰۷** |
| کلید روشن/تاریک با تغییر `data-theme-mode` | **۲۰۷/۲۰۷** |
| بازگشت به حالت اولیه پس از کلیک دوم | ۲۰۷/۲۰۷ |

> یافته و اصلاح: در ۱۱ صفحه احراز هویت و صفحه محصول، دکمه زبان هیچ فهرستی برای
> انتخاب نداشت (فقط یک دکمه بی‌اثر بود). هر دو اکنون از همان سوییچر فهرست‌دار
> هدر استفاده می‌کنند و بازرسی روی همه صفحه‌ها سبز است.

## ۵) بازرسی واکنش‌گرایی (`qa:responsive`)

| بررسی | نتیجه |
| --- | --- |
| `meta viewport` در همه صفحه‌ها | ۲۰۷/۲۰۷ |
| جدول بیرون از ظرف اسکرول | ۰ |
| صفحه بدون دکمه دراور (صفحه‌های دارای سایدبار) | ۰ |
| منوی موبایل صفحه محصول | ✔ دکمه منو + باز/بسته شدن با `aria-expanded` |
| نقاط شکست | ۳۲۰ / ۳۷۵ / ۴۱۴ / ۵۷۶ / ۷۶۸ / ۹۹۲ / ۱۲۰۰ / ۱۴۴۰ / ۱۶۸۰ / ۱۹۲۰ |

قواعد لبه‌ای در `src/scss/base/_breakpoints.scss`:

- **≤ ۴۱۴px**: گاتر کمتر، هدر ستونی، نوار ابزار تاشو، فونت جدول کوچک‌تر، کانبان تک‌ستونه، هدف لمسی ≥ ۴۰px
- **≤ ۳۶۰px**: ردیف KPI تک‌ستونه، آیکون کوچک‌تر، فاصله‌های فشرده‌تر
- **≥ ۱۹۲۰px**: سقف عرض محتوا برای خوانایی روی مانیتور بزرگ
- **landscape کوتاه**: دراور با اسکرول مستقل

## ۶) بازرسی ترجمه (`qa:i18n`)

```text
keys used      375
fa missing     0
en missing     0
ar missing     0
```

## ۷) بررسی‌های مرورگری که در محیط ساخت انجام‌شدنی نیست

این پروژه در محیط بدون مرورگر ساخته شده است؛ موارد زیر باید در مرورگر خودتان تأیید شوند
(پیش‌نمایش زنده: سرور توسعه روی پورت ۵۱۷۳):

1. نبود اسکرول افقی در ۳۲۰ / ۳۷۵ / ۴۱۴ / ۷۶۸ پیکسل
2. باز و بسته شدن دراور سایدبار با لمس روی پوشش تیره
3. برگشتن جدول‌ها داخل ظرف خودشان، نه کل صفحه
4. عبور همه صفحه‌ها از کنسول بدون خطا (منوی DevTools → Console)

> اجرای مرورگر بدون رابط (Playwright/Chromium) در این محیط ممکن نبود؛ بررسی ایستا،
> اجرای شبیه‌سازی‌شده و کلیک واقعی روی کنترل‌ها جای آن را گرفته و هیچ مانع ساختاری‌ای
> گزارش نشده است.

## ۸) نمونه‌گیری بینایی

| صفحه | وضعیت |
| --- | --- |
| `index.html` — صفحه محصول | ✔ ۱۲ بخش داخل ظرف، منوی موبایل، شمارنده از سرویس، آیکون در همه بخش‌ها |
| `preview.html` — پیش‌نمایش زنده | ✔ گالری ۱۰ دمو، ۶ چیدمان و ۶ پالت از `theme` و `config.demos` |
| `auth/*` — ۱۱ حالت | ✔ فیلد آیکون‌دار، نمایش/مخفی رمز، سنجه قدرت رمز، جعبه حساب آزمایشی، یادداشت امنیتی |
| `dashboards/*` — ۱۰ داشبورد | ✔ KPI، نمودار، جدول و فعالیت اختصاصی |
| `ui/*` — ۲۱ صفحه کیت رابط | ✔ نمونه‌های زنده با کامپوننت واقعی؛ ۴۰۲ آیکون جستجوپذیر |
| `docs/*` — ۲۳ موضوع مستند | ✔ رندر از مارک‌داون، بلوک کد escape‌شده، فهرست کنار صفحه |
| `widgets.html` — کتابخانه ابزارک | ✔ KPI، نمودار، جدول و تایم‌لاین با داده سرویس |

## ۹) نتیجه

```text
✔ QA passed — links, assets, structure, CSS coverage and SEO are clean.
✔ 207/207 pages booted with data on screen
✔ 207/207 pages switch language, theme and direction from their chrome
✔ responsive audit passed — no structural blockers found.
✔ package ready — release/NOVAADMIN-1.0.0.zip
```

تمام بندهای §۱۱۷ پرسش‌نامه ساخت که در محیط بدون مرورگر قابل ارزیابی‌اند، پاس شده‌اند:
نصب، ساخت، کنسول و import، لینک‌ها، تصویرها، آیکن‌ها، CSS، JS، ترجمه، کلیدهای
سراسری و بررسی واکنش‌گرایی ساختاری.

_تاریخ اجرا: ۱۴۰۵/۰۷/۰۱ · ۲۰۲۶-۰۹-۲۳_
