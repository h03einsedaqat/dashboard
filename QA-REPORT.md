# گزارش کنترل کیفیت نهایی — NOVAADMIN v1.0.0

این گزارش نتیجه اجرای واقعی زنجیره ساخت و کنترل کیفیت روی نسخه ۱٫۰٫۰ است.
دستور بازتولید: `npm run qa:all`

```bash
npm install        ✔ نصب وابستگی‌ها بدون هشدار مسدودکننده
npm run build      ✔ ۲۰۶ صفحه تولید و مرتب شد
npm run qa:links   ✔ همه بررسی‌ها پاس شد
npm run qa:smoke   ✔ ۱۵۰/۱۵۰ سناریو
npm run qa:responsive ✔ بدون مانع ساختاری
npm run package    ✔ بسته تحویل ساخته شد
```

## ۱) ساخت و خروجی

| مورد | نتیجه |
| --- | --- |
| صفحه‌های تولیدشده | **۲۰۶** (۷ بخش، ۲۳ موضوع مستندات) |
| صفحه در `dist/` | **۲۰۷** (شامل `index.html`) |
| بایت خروجی | ۲۴٬۳۹۷ کیلوبایت |
| دارایی‌ها | ۶۸ فایل (CSS، JS، فونت، تصویر برداری) |
| CSS | ۱ فایل، ۴۸۶ کیلوبایت (۶۶ کیلوبایت gzip) |
| JS | ۹ فایل چانک‌شده (`modules`, `apps`, `ai`, `content`, `vendor-*`) |
| بسته ZIP | ۶۷۰ فایل، ۳٫۹ مگابایت (فشرده) |

## ۲) بررسی لینک، دارایی و ساختار (`qa:links`)

| بررسی | نتیجه |
| --- | --- |
| لینک و دارایی درون صفحه‌ای | **۴۵٬۷۶۸** بررسی‌شده، **۰** شکسته |
| توکن‌های قالبی باقی‌مانده (`{{…}}`) | ۰ |
| کلاس‌های استفاده‌شده | ۴۳۳ کلاس، **۰** بدون استایل (از ۵٬۵۷۸ قاعده CSS) |
| شناسه‌های تکراری | ۰ |
| `<main>` و `<h1>` در همه صفحه‌ها | ✔ |
| تصویر بدون `alt` | ۰ |
| jQuery / `data-bs-*` / `var` | ۰ |
| نشانگرهای `console.log` / `debugger` | ۰ |
| فونت و تصویر مفقود در CSS | ۰ |
| `sitemap.xml` | ۱۷۹ URL + `robots.txt` |

## ۳) اجرای واقعی کنترل‌کننده‌ها (`qa:smoke`)

**۱۵۰ سناریو از ۱۵۰ سناریو** بدون خطا در DOM شبیه‌سازی‌شده اجرا شد؛ شامل همه داشبوردها، ماژول‌ها، کارگاه هوش مصنوعی، جدول‌های داده، تقویم، کانبان، چت، فاکتور، احراز هویت، مستندات، صفحات سیستمی و صفحه‌های `preview.html` و `widgets.html`.
هیچ خطای `import`، تابع ناموجود یا رندر ناموفقی گزارش نشد.

## ۴) بازرسی واکنش‌گرایی (`qa:responsive`)

| بررسی | نتیجه |
| --- | --- |
| `meta viewport` در همه صفحه‌ها | ۲۰۷/۲۰۷ |
| جدول بیرون از ظرف اسکرول | ۰ |
| صفحه بدون دکمه دراور (صفحه‌های دارای سایدبار) | ۰ |
| نقاط شکست | ۳۲۰ (۳۵۹٫۹۸) / ۳۷۵ / ۴۱۴ / ۵۷۶ / ۷۶۸ / ۹۹۲ / ۱۲۰۰ / ۱۴۴۰ / ۱۹۲۰ |
| توکن‌های SCSS مربوط به عرض‌های کوچک و بزرگ | ۳۲۰، ۳۵۹، ۴۱۴، ۱۶۰۰، ۱۸۰۰، ۱۹۲۰ |

قواعد لبه‌ای در `src/scss/base/_breakpoints.scss`:

- **≤ ۴۱۴px**: گاتر کمتر، هدر صفحه‌ای ستونی، نوار ابزار تاشو، جدول با فونت کوچک‌تر، کانبان تک‌ستونه، هدف لمسی ≥ ۴۰px
- **≤ ۳۶۰px**: ردیف KPI تک‌ستونه، اندازه آیکن کوچک‌تر، فاصله‌های فشرده‌تر
- **≥ ۱۹۲۰px**: سقف عرض محتوا برای خوانایی روی مانیتورهای بزرگ
- **landscape کوتاه**: دراور با اسکرول مستقل

## ۵) بررسی‌های مرورگری که در محیط ساخت انجام‌شدنی نیست

این پروژه در محیط بدون مرورگر ساخته شده است؛ موارد زیر باید در مرورگر خودتان تأیید شوند
(پیش‌نمایش زنده: سرور توسعه روی پورت ۵۱۷۳):

1. نبود اسکرول افقی در ۳۲۰ / ۳۷۵ / ۴۱۴ / ۷۶۸ پیکسل
2. باز و بسته شدن دراور سایدبار با لمس روی پوشش تیره
3. برگشتن جدول‌ها داخل ظرف خودشان، نه کل صفحه
4. عبور همه صفحه‌ها از کنسول بدون خطا (منوی DevTools → Console)

> در زمان ساخت، اجرای مرورگر بدون رابط (Playwright/Chromium) در این محیط ممکن نبود؛ بررسی ایستا و اجرای شبیه‌سازی‌شده جای آن را گرفته است و هیچ مانع ساختاری‌ای گزارش نشد.

## ۶) نمونه‌گیری بینایی

| صفحه | وضعیت |
| --- | --- |
| `preview.html` — پیش‌نمایش زنده با ۱۰ دمو، ۶ چیدمان و ۶ پالت | ✔ گالری‌ها در زمان اجرا از `theme` و `config.demos` ساخته می‌شوند |
| `widgets.html` — کتابخانه ابزارک | ✔ KPI، نمودار، جدول و تایم‌لاین با داده سرویس |
| `docs/*.html` — ۲۳ موضوع مستند | ✔ از مارک‌داون رندر شده، بلوک کد escape‌شده، فهرست کنار صفحه |
| `dashboards/*` — ۱۰ داشبورد | ✔ KPI، نمودار، جدول و فعالیت اختصاصی |
| `auth/*` — ۱۱ حالت | ✔ یک `h1` پنهان برای دسترس‌پذیری، کارت `h2` |
| صفحه محصول `index.html` | ✔ دوازده بخش، CTA، قیمت‌گذاری، SEO و FAQ |

## ۷) نتیجه

```text
✔ QA passed — links, assets, structure, CSS coverage and SEO are clean.
✔ 150/150 scenarios booted cleanly
✔ responsive audit passed — no structural blockers found.
✔ package ready — release/NOVAADMIN-1.0.0.zip
```

تمام بندهای §۱۱۷ پرسش‌نامه ساخت که در محیط بدون مرورگر قابل ارزیابی‌اند، پاس شده‌اند:
نصب، ساخت، کنسول و import، لینک‌ها، تصویرها، آیکن‌ها، CSS، JS و بررسی واکنش‌گرایی ساختاری.

_تاریخ اجرا: ۱۴۰۵/۰۶/۳۱ · ۲۰۲۶-۰۹-۲۲_
