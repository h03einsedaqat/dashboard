# نقشه راه فروش NOVAADMIN در rtl-theme.com

> این سند داخلی است و بخشی از بسته فروش به خریدار نیست.
> تاریخ: ۱۴۰۵/۰۷/۰۱ — نسخه قالب: ۱٫۰٫۰ (به‌همراه اصلاحات ممیزی مرورگری)

---

## بخش ۱ — وضعیت فعلی (چه چیزی همین حالا آماده فروش است)

| شاخص | مقدار |
| --- | --- |
| صفحه HTML مستقل | **۲۰۶** (+ صفحه محصول `index.html`) |
| داشبورد تخصصی | **۱۰** (آنالیتیکس، فروشگاهی، CRM، SaaS، مالی، پروژه، HR، پشتیبانی، هوش مصنوعی، لجستیک) |
| کیت رابط کاربری | ۲۱ صفحه، ۴۰+ کامپوننت با همه حالت‌ها |
| مستندات | ۲۳ موضوع |
| زبان‌ها | فارسی / انگلیسی / عربی — با تغییر زنده، اعداد و تاریخ بومی |
| چیدمان و ظاهر | ۶ چیدمان، ۶ سبک سایدبار، ۶ پالت، روشن/تاریک/سیستم، سه تراکم، سه اندازه فونت |
| تقویم | شمسی و میلادی |
| فناوری | Vite 7، Bootstrap 5.3 (بدون jQuery)، ApexCharts، SCSS توکن‌محور، Vazirmatn متغیر |

**اصلاحات تازه (ممیزی مرورگری):** منوها، مودال‌ها و دراور موبایل که در نسخه اول
کار نمی‌کردند تعمیر شدند؛ چیدمان دو ستونی بازنویسی شد؛ محورهای نمودار تمیز
شدند؛ صفحه لندینگ کامل شد؛ و تغییر زبان اکنون کل محتوا (متن، عدد، تاریخ، جدول،
نمودار) را جابه‌جا می‌کند. جزئیات و سنجه‌ها در `QA-REPORT.md` بخش ۱۰.

---

## بخش ۲ — گام‌های بعدی برای فروش (به ترتیب اولویت)

### گام ۱ — بسته‌بندی و اولین لیستینگ (امروز، ۲ تا ۴ ساعت)

1. **ساخت بسته نهایی**
   ```bash
   npm install
   npm run build      # dist/ تک‌سطحی و آماده آپلود
   npm run package    # ZIP تحویل
   ```
2. **تصاویر لیستینگ (مهم‌ترین عامل کلیک)**: ۶ تا ۸ تصویر ۱۲۰۰×۹۰۰ از
   آنالیتیکس، فروشگاهی، CRM، تقویم شمسی، کیت UI و صفحه محصول — در هر دو حالت
   روشن و تاریک.
3. **یک ویدیوی ۳۰ ثانیه‌ای**: تغییر زنده تم، رنگ، چیدمان و زبان. این ویدیو
   بیشتر از هر متنی می‌فروشد.
4. **متن لیستینگ دو زبانه** (فارسی + انگلیسی): تیتر کوتاه، سه بند توضیح،
   فهرست امکانات، تگ‌ها (`RTL`، `ادمین پنل فارسی`، `Bootstrap 5`، `Vazirmatn`،
   `Jalali`، `AI`).
5. **قیمت‌گذاری سه‌سطحی** (همان ساختار صفحه `system/pricing.html`):
   نسخه ساده / کامل / توسعه‌دهنده با سفارشی‌سازی و SLA.
6. **چک‌لیست rtl-theme**: لایسنس شفاف، ساختار پوشه‌ها، تاریخ آخرین به‌روزرسانی،
   وعده پشتیبانی (۶ یا ۱۲ ماه)، سیاست بازگشت وجه.

### گام ۲ — رسیدن به سطح «تاپ فروش» (۲ تا ۴ روز)

1. **دموی زنده میزبانی‌شده** — بدون این، نرخ تبدیل تقریباً نصف می‌شود.
   روی Vercel/Netlify آپلود کنید و لینک دمو را در لیستینگ بگذارید.
2. **صفحه محصول به‌جای صفحه لندینگ**: `index.html` اکنون سریع، مراحل شروع،
   آمار و FAQ دارد؛ یک بخش ویدیو/دموی زنده و سه «نظر خریدار» واقعی به آن اضافه
   کنید (نظر ساختگی نگذارید).
3. **«شروع در ۵ دقیقه»**: یک فایل `START-HERE.html` کوتاه‌تر و تصویری‌تر؛
   خریدار ایرانی معمولاً نمی‌خواهد کل مستندات را بخواند.
4. **بازبینی ترجمه‌ها توسط انسان**: متن انگلیسی و عربی را یک نفر بازخوانی کند.
   ترجمه ماشینیِ ناهموار سطح محصول را پایین می‌آورد.
5. **تست روی مرورگرهای واقعی**: Safari آیفون و Chrome اندروید روی دستگاه
   واقعی (ریسپانسیو تا ۳۶۰ پیکسل سنجیده شده است اما شبیه‌سازی مرورگر جای
   دستگاه واقعی را نمی‌گیرد).
6. **حذف وعده‌های اضافه**: اگر فایل FIGMA ندارید، در لیستینگ از آن نام نبرید.

### گام ۳ — تمایز و پشتیبانی (۱ تا ۲ هفته)

1. **سه ویدیوی آموزشی فارسی** (نصب، شخصی‌سازی، اتصال به API) — بازگشتِ نرخ
   پشتیبانی را نصف می‌کند.
2. **CHANGELOG حرفه‌ای و نقشه راه عمومی**: خریدار باید بداند چه چیزی در
   نسخه بعدی می‌آید (`src/data/system.js` → بخش changelog همین را نشان می‌دهد).
3. **الگوهای آماده Laravel / API**: نگارش یک نمونه‌ی کوتاه `laravel.md` که
   نگاشت سرویس‌ها را نشان دهد — بازار هدف rtl-theme اغلب جنگو/لاراول است.
4. **سیستم پاسخ‌دهی**: ایمیل پشتیبانی + پاسخ‌های آماده برای ۱۰ پرسش پرتکرار
   (همان محتوای FAQ صفحه محصول).
5. **مستندسازی سایت‌های فارسی**: فهرست ۵ صفحه که خریدار بیشترین درخواست
   تغییر را دارد (ورود، داشبورد، فهرست‌ها، فرم‌ها، گزارش‌ها) و نمونه کد.

### گام ۴ — پیوسته

1. **CI برای کیفیت**: `npm run qa:all` را در GitHub Actions بگذارید تا هر
   تغییر قبل از انتشار بررسی شود (و یک آزمون مرورگری واقعی به آن اضافه شود؛
   آزمون‌های DOM شبیه‌سازی‌شده باگ CSS را نمی‌بینند).
2. **کاهش وزن خروجی**: CSS نهایی ۵۶۳ کیلوبایت (gzip ≈ ۷۲ کیلوبایت) است؛
   با حذف آیکون‌های استفاده‌نشده Bootstrap Icons می‌توان به ≈ ۴۵ کیلوبایت رسید.
3. **جمع‌آوری نظر خریداران** در ۳۰ روز اول و انتشار نسخه ۱٫۱ بر پایه آن.
4. **لیستینگ مکمل**: انتشار همان قالب (با همان فایل‌ها، توضیح متفاوت) در
   مارکت‌های دیگر؛ برای rtl-theme.com اولویت با امتیاز و نظرهای اولیه است.

---

## بخش ۳ — نقاط ضعف شناخته‌شده (صادقانه)

| مورد | وضعیت | پیشنهاد |
| --- | --- | --- |
| متون بلند مستندات و `preview.html` | فارسی می‌مانند (طبق طراحی) | برای نسخه انگلیسی، ترجمه انسانی سفارش دهید |
| نام‌های نمونه (افراد، شرکت‌ها، محصولات) | عمداً ترجمه نشده‌اند | در مستندات توضیح دهید که داده نمایشی است |
| آزمون‌های `tools/qa-*.mjs` | شبیه‌ساز DOM هستند، CSS را ارزیابی نمی‌کنند | یک آزمون مرورگری (Playwright) به CI اضافه شود |
| ویدیو و دموی زنده | ندارد | گام ۲ بخش ۱ و ۲ |
| ترجمه عربی | بازبینی انسانی نشده | گام ۲ بخش ۴ |

---

## بخش ۴ — معیار «آماده فروش» (تعریف انجام‌شده)

- [x] همه صفحه‌ها بدون خطای زمان اجرا بالا می‌آیند.
- [x] هیچ صفحه‌ای سرریز افقی ندارد (۳۹۰ / ۷۶۸ / ۱۰۲۴ / ۱۴۴۰).
- [x] تغییر زبان، تم، رنگ، جهت، تراکم، فونت، چیدمان و سبک سایدبار بدون خرابی
      اعمال می‌شود.
- [x] منوها، مودال‌ها، دراور، توست، جداول و نمودارها در هر شش چیدمان کار
      می‌کنند.
- [x] بازگشت به فارسی، متن اصلی را مو‌به‌مو برمی‌گرداند.
- [x] `npm run build` بدون خطا و بدون هشدار مسدودکننده اجرا می‌شود.
- [ ] دموی زنده میزبانی‌شده (گام ۲).
- [ ] ویدیو معرفی (گام ۲).
- [ ] بازبینی انسانی ترجمه‌های en/ar (گام ۲).
